namespace Manifold.Tiled
{
    /// <summary>
    /// The encoding used to encode the tile layer data.
    /// </summary>
    public enum Encoding
    {
        None,
        CSV,
        Base64,
    }
}
