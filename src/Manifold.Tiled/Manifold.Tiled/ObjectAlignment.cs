namespace Manifold.Tiled
{
    /// <summary>
    /// Controls the alignment for tile objects.
    /// </summary>
    public enum ObjectAlignment : byte
    {
        Unspecified,
        TopLeft,
        Top,
        TopRight,
        Left,
        Center,
        Right,
        BottomLeft,
        BottomCenter,
        BottomRight,
    }
}
